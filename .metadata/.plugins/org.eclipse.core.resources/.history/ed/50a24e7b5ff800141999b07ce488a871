package bingo;

import bingo.Letter;
import bingo.interfaces.Card;

public class CardPrinter {

	public void print(Card card, boolean withCovers) {
		System.out.println(" B  I  N  G  O ");
		for (int i = 0; i < 5; i++) { // TODO: Don't hardcode 5, get it from the card
			System.out.println(getRow(card, withCovers, i));
		}
	}
	
	private String getRow(Card card, boolean withCovers, int row) {
		StringBuilder builder = new StringBuilder();
		for (Letter letter : Letter.values()) { 
			if (!withCovers || !card.getSpace(letter, row).isCovered()) {
				int value = card.getSpace(letter, row).getValue();
				if (value < 9) {
					builder.append(" ");
				}
				if (value == -1) {
					builder.append("F");
				}
				else {
					builder.append(value);
				}
				builder.append(" ");
			}
			else {
				builder.append(" X  ");
			}

		}
		return builder.toString();
	}
	
}
